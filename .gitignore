# Dependencies (reinstalled via `npm install`, no need to version them)
node_modules/

# Build output (regenerated by build-installer.bat / release.bat)
release/
dist/

# Auto-downloaded by the build scripts - re-fetched each time, no need to track
tools/yt-dlp.exe

# Never commit this - it's your GitHub upload token
.env

# OS/editor cruft
.DS_Store
Thumbs.db
.vscode/
*.log
